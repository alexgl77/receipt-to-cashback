# Copy this file to `.env` and fill in real values. `.env` is gitignored.

# Gemini 2.0 Flash — free tier
# Get a key at https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Optional: Hugging Face token (only needed for some gated models)
HF_TOKEN=
