# Core
python-dotenv>=1.0.0
pydantic>=2.0.0
Pillow>=10.0.0

# Data wrangling + viz (Week 3-4)
pandas>=2.0.0
numpy>=1.24.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Stats + classical ML (Week 4-5)
scikit-learn>=1.3.0
scipy>=1.11.0

# Vector DB + multilingual embeddings (Week 8)
faiss-cpu>=1.7.4
sentence-transformers>=2.5.0

# LLM API — new SDK (Week 9). Uses `from google import genai`.
# (Not the deprecated `google-generativeai` package.)
google-genai>=0.3.0

# Streamlit web app (self-taught — gap from bootcamp)
streamlit>=1.30.0
